services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smithery-2api-deno
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NGINX_PORT=${NGINX_PORT:-8088}
    ports:
      - "${NGINX_PORT:-8088}:8088"
    command: deno task start
